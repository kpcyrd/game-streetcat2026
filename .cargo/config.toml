[build]
target = "riscv32ec-unknown-none-elf.json"
rustflags = [
    # Helpful for size optimizations
    #"-C", "llvm-args=--inline-threshold=0",
]

[target."riscv32ec-unknown-none-elf"]
runner = "wlink flash"

[unstable]
build-std = ["core"]
json-target-spec = true
